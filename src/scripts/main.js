import {
  // loadHistory, maybe later
  setIndexButtons,
  setIndexNav
} from "./load.js";

document.addEventListener("DOMContentLoaded", () => {
  setIndexButtons();
  setIndexNav();
  // loadHistory(); maybe later
})